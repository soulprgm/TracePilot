# Copy this file to .env and choose a local PostgreSQL password.
POSTGRES_PASSWORD=change-me-for-local-development

# Optional: enables model-backed root-cause analysis. Without this key,
# TracePilot still returns a clearly labelled built-in diagnosis.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-6-luna
